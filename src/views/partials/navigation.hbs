<nav class="navbar navbar-expand-lg navbar-dark p-2 bg-black">
  <div class="container">
    <a class="navbar-brand" href="/">RHWeb</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto align-items-center">
        {{#if user}}
          <li class="nav-item me-2">
            <a class="btn btn-primary btn-sm" href="/dashboard" role="button">Dashboard</a>
          </li>
        {{else}}
          <li class="nav-item">
            <a class="nav-link" href="/about">Acerca de</a>
          </li>
        {{/if}}
      </ul>

      <ul class="navbar-nav ms-auto align-items-center">
        {{#if user}}
          <li class="nav-item me-2">
            <a class="btn btn-outline-light btn-sm" href="/profile" role="button">Mi perfil</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-light btn-sm" href="/auth/logout" role="button">Cerrar sesión</a>
          </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>
